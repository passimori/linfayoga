function purchaseCourse(courseName, price) {
  alert(`You are about to purchase the course: ${courseName} for $${price}.`);
  // Add your payment integration here, such as PayPal or Stripe.
  console.log(`Redirecting to payment for ${courseName}...`);
}
